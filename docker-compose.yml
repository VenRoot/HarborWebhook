version: '3.8'

services:
  build-service:
    build:
      context: .
      dockerfile: Dockerfile.build
    volumes:
      - ./bin:/app/bin  # Binde das Host-Verzeichnis an den Container
    command: ["npm", "run", "build"]  # Führe den Build-Prozess aus

# Optionaler zusätzlicher Dienst für die Kompilierung
# ohne dass der Container dauerhaft läuft
  build:
    image: build-service  # Verwende das bereits gebaute Image
    volumes:
      - ./bin:/app/bin  # Binde das Host-Verzeichnis an den Container
    command: ["npm", "run", "build"]
